/*
const filterToggle = $('.js-toggle-filter');

filterToggle.click((e) => {
  const target = $(e.currentTarget);
  if (target.hasClass('is-open')) {
    target.removeClass('is-open');
  } else {
    filterToggle.removeClass('is-open');
    target.addClass('is-open');
  }
});

const filterContentField = $('.js-filter-content-field');

$(document).mouseup((e) => {
  if (!filterContentField.is(e.target) && filterContentField.has(e.target).length === 0) {
    $('.js-toggle-filter').removeClass('is-open');
  }
});
*/
